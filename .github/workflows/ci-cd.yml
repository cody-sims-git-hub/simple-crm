name: CI / Deploy

# Solo-dev flow:
#   * feature/fix branches: push freely — CI runs nothing (you test locally).
#   * open a PR to main: fast lint + audit only (sanity net + a place to delay/review).
#   * push/merge to main: full build + test, then production deploy.
#   * deploy ONLY runs from main, and only after the full suite is green.
#   * docs-only changes (Markdown/LICENSE) skip the pipeline entirely.
#   * merged PR branches are auto-deleted via the repo's delete_branch_on_merge setting.
on:
  push:
    branches: [ main ]
    paths-ignore:
      - '**.md'
      - 'docs/**'
      - 'LICENSE'
  pull_request:
    branches: [ main ]
    paths-ignore:
      - '**.md'
      - 'docs/**'
      - 'LICENSE'

jobs:
  # ── PR only: lightweight validation. No npm build, no test suite, no deploy. ──
  pr-check:
    name: PR validation (lint + audit)
    if: github.event_name == 'pull_request'
    runs-on: ubuntu-latest
    permissions:
      contents: read
    # Pushing more commits to an open PR cancels the superseded check.
    concurrency:
      group: pr-${{ github.ref }}
      cancel-in-progress: true
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up PHP 8.4
        uses: shivammathur/setup-php@v2
        with:
          php-version: '8.4'
          coverage: none

      - name: Install Composer dependencies
        run: composer install --no-interaction --prefer-dist --no-progress

      # Dependency vulnerability scan (SCA) against the Composer advisory database.
      - name: Audit PHP dependencies
        run: composer audit

      # Static style gate — fails if any file drifts from the project's Pint ruleset.
      - name: Check code style (Pint)
        run: ./vendor/bin/pint --test

  # ── main only: authoritative full build + test gate before deploy. ──
  ci:
    name: Test & Build
    if: github.event_name == 'push' && github.ref == 'refs/heads/main'
    runs-on: ubuntu-latest
    permissions:
      contents: write   # badge push to the `badges` branch
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up PHP 8.4
        uses: shivammathur/setup-php@v2
        with:
          php-version: '8.4'
          coverage: none

      - name: Install Composer dependencies
        run: composer install --no-interaction --prefer-dist --no-progress

      - name: Audit PHP dependencies
        run: composer audit

      - name: Check code style (Pint)
        run: ./vendor/bin/pint --test

      - name: Set up Node 22
        uses: actions/setup-node@v4
        with:
          node-version: '22'

      - name: Install npm dependencies
        run: npm install --no-audit --no-fund

      # Build gate — front-end assets must compile (same build the image runs).
      - name: Build front-end assets
        run: npm run build

      - name: Prepare test environment
        run: |
          cp .env.example .env
          php artisan key:generate

      # Full PHPUnit suite (in-memory SQLite). --log-junit feeds the dynamic badge.
      - name: Run test suite
        run: php artisan test --log-junit junit.xml

      # Publish the live PHPUnit count to the `badges` branch (shields endpoint).
      # The job is already main-push-only, so no per-step guard is needed. Uses the
      # built-in GITHUB_TOKEN — no PAT or external gist required.
      - name: Publish test-count badge
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          total=$(grep -o '<testcase ' junit.xml | wc -l | tr -d ' ')
          echo "PHPUnit test count: $total"
          printf '{"schemaVersion":1,"label":"PHPUnit","message":"%s passing","color":"3FB950"}' "$total" > tests.json
          tmp=$(mktemp -d); cp tests.json "$tmp/"; cd "$tmp"
          git init -q && git checkout -q -b badges && git add tests.json
          git -c user.name="github-actions[bot]" \
              -c user.email="github-actions[bot]@users.noreply.github.com" \
              commit -q -m "chore: update PHPUnit test-count badge ($total passing)"
          git push -q -f "https://x-access-token:${GH_TOKEN}@github.com/${GITHUB_REPOSITORY}.git" badges

  deploy:
    name: Deploy to production
    # HARD GATE: cannot start unless the full CI job succeeded.
    needs: ci
    if: github.event_name == 'push' && github.ref == 'refs/heads/main'
    runs-on: ubuntu-latest
    # Serialize and NEVER cancel a deploy mid-flight.
    concurrency:
      group: production-deploy
      cancel-in-progress: false
    steps:
      # The VPS is reachable only over Tailscale (CGNAT address), so the runner
      # joins the tailnet first using a reusable, pre-authorized auth key.
      - name: Connect to Tailscale
        uses: tailscale/github-action@v3
        with:
          authkey: ${{ secrets.TAILSCALE_AUTHKEY }}

      # SSH auth is handled by Tailscale SSH (the tailnet ACL authorizes the
      # CI node -> VPS as this user), so no SSH private key is needed here.
      - name: Deploy over Tailscale SSH
        env:
          HOST: ${{ secrets.VPS_SSH_HOST }}
          USER: ${{ secrets.VPS_SSH_USER }}
          DIR:  ${{ secrets.VPS_DEPLOY_DIR }}
        # $USER/$HOST/$DIR are expanded by the runner shell (from `env:` above)
        # and baked into the command string sent to the VPS. A rebuilt image
        # makes `up -d` recreate the container, which re-runs the serversideup
        # boot automations (config:cache + migrate) against .env.
        run: |
          ssh -o StrictHostKeyChecking=accept-new "$USER@$HOST" \
            "set -e; cd '$DIR'; git pull --ff-only; docker compose build; docker compose up -d; docker image prune -f"

      # Confirm the new container is actually serving before calling it a success.
      - name: Verify production health
        run: |
          for i in $(seq 1 10); do
            code=$(curl -s -o /dev/null -w '%{http_code}' https://demo.simsdigitalpartners.com/up || true)
            echo "attempt $i: HTTP $code"
            [ "$code" = "200" ] && exit 0
            sleep 6
          done
          echo "Health check never returned 200"; exit 1
